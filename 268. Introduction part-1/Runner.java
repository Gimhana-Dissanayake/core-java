public class Runner {

    // Collection : collection is an object , it able to represent or store a group
    // of other objects.

    // NOTE: In java ,collection objects are able to store only objects reference
    // values, collection objects are not going to store primitive data directly

    // If we want to store primitive data in collection object then we have to use
    // the fallowing procedure

}
