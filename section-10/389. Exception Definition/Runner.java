public class Runner {

}

// Exception is an unexpected event occured at runtime, which may be provided by
// the users while entering dynamic input, which may be providied by the
// database engine while executing sql queries in jdbc applications, which may
// be provided the network when we establish connection between local machine
// and remote machine in distributed applications, ... causes abnormal
// termination to the applications.