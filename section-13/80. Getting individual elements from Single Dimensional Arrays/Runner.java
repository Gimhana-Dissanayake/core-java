public class Runner {

}

// In java applications, to get length / size / no of elements of an array we
// have to use a predefined variable in the form of 'length'

// In java applications, if we want to read an elemet from an array then we have
// to use the following format
// Ex: arrayRefVar[index]

// While getting elements form an array on the basis of index values, if we
// provide an index value which is in out side range of the array indexes then
// JVM will raise an exception like "java.lang.ArrayIndexOutOfBoundsException"