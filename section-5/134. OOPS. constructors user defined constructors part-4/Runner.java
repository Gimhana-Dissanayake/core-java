class Runner {
    // local variable's data will be stored in stack memory
    // Instance variable's data will be stored in heap memory

    // in general objects are created in heap memory.so these variables have access
    // though out the class.
    // stack memory is only available throught out the method

}